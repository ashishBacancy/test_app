<%= form_with(model: iron) do |form| %>
  <% if iron.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(iron.errors.count, "error") %> prohibited this iron from being saved:</h2>

      <ul>
        <% iron.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
